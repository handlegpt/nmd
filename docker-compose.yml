version: '3.8'

services:
  # Main React Native Expo app
  nomadnow-app:
    build: .
    ports:
      - "19000:19000"  # Expo development server
      - "19001:19001"  # Expo DevTools
      - "19002:19002"  # Expo DevTools web interface
    environment:
      - NODE_ENV=development
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
      - EXPO_PUBLIC_SUPABASE_URL=${EXPO_PUBLIC_SUPABASE_URL}
      - EXPO_PUBLIC_SUPABASE_ANON_KEY=${EXPO_PUBLIC_SUPABASE_ANON_KEY}
    stdin_open: true
    tty: true
    restart: unless-stopped

  # Optional: Local Supabase development environment
  # Uncomment if you want to run Supabase locally
  # supabase:
  #   image: supabase/postgres-meta:latest
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_PASSWORD: your_password
  #     POSTGRES_DB: nomadnow
  #   volumes:
  #     - supabase_data:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   supabase_data: 